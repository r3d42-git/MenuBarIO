# MenuBarUSB-TB 0.1.1

This release improves installation without changing the app's device
discovery or privacy behavior.

## Installation

- New compact installer DMG with a clear drag-and-drop path from
  `MenuBarUSB-TB.app` to **Programme** (`/Applications`)
- Deliberately arranged Finder window, instruction and background instead of
  an unarranged folder

## Compatibility

**This release supports Apple Silicon Macs (`arm64`) running macOS 13 or newer.
Intel Macs are not supported.**

---

# MenuBarUSB-TB 0.1.1 – Deutsch

Diese Version verbessert die Installation, ohne die Geräteerkennung oder das
Datenschutzverhalten der App zu ändern.

## Installation

- Neues kompaktes Installer-DMG mit einem klaren Drag-and-drop-Weg von
  `MenuBarUSB-TB.app` zu **Programme** (`/Applications`)
- Bewusst angeordnetes Finder-Fenster mit Anweisung und Hintergrund statt eines
  ungeordneten Ordners

## Kompatibilität

**Diese Version unterstützt Apple-Silicon-Macs (`arm64`) mit macOS 13 oder
neuer. Intel-Macs werden nicht unterstützt.**
